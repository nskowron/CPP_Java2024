IShape:	//Canvas works on it
- getX(), getY() //middle point of shape
- getTransforms() //for scale, rotate and translate
- Resize() //without keeping the aspect ratio
- setFill(Paint paint)
//Rectangle, Triangle, Ellipse//

Canvas extends Pane:
- IShape selectedShape

OptionButton extends Button:
- icon //is set in constructor

SelectButton extends OptionButton:
- OnAction:
	- sets all shapes' in canvas onMousePressed
	- sets canvas onMouseDragged
